{
  "name": "drugvokrug-server",
  "version": "0.1.0",
  "private": true,
  "description": "Private server emulator for the ДругВокруг (drugvokrug.ru) Android app, protocol reverse-engineered from APK 2.3.1 (RubyLight/rubylight custom binary protocol).",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node test/run.js"
  },
  "engines": {
    "node": ">=22.5.0"
  }
}
